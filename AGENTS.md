- После любых правок сделать коммит
- Запрещены файлы с кодом длиннее 300 строк
- идемпотентность критично важна
- не нужны fallback, всё должно работать так как задумано
- Диагностика и чтение debug-логов: `docs/diagnostics-runbook.md`
- Если что-то критичное поменялось и необходимо переустановить orc для локальной работы, запусти uv tool install
- Если починил баг проверь аналогичные куски кода на аналогичную ошибку. Если это уместно сделай переиспользование и дедупликацию, если неуместно то тоже почини.
- любой запущенный процесс должен быть под контролем, инвариант: недопустимо оставить беспризорный процесс

