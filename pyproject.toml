[project]
name = "orc"
version = "0.1.0"
description = "ORC backlog orchestrator for Cursor Agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "prompt_toolkit>=3.0.51",
]

[tool.uv]
package = false
