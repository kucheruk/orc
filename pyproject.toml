[project]
name = "orc"
version = "0.1.0"
description = "ORC backlog orchestrator for Cursor Agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "rich>=13.9.4",
  "textual>=0.88.1",
]

[tool.uv]
package = false
